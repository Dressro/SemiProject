<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.project.fp.member">

	<select id="selectList" resultType="AnimalDto">
		SELECT ANIMAL_NO, ANIMAL_NAME, ANIMAL_GEN, ANIMAL_TYPE,
			   ANIMAL_AGE, ANIMAL_WEIGHT, ANIMAL_UNQ, MEMBER_ID
		FROM ANIMAL
		ORDER BY ANIMAL_NO DESC
	</select>
	
	<select id="selectOne" parameterType="int" resultType="AnimalDto">
		SELECT ANIMAL_NO, ANIMAL_NAME, ANIMAL_GEN, ANIMAL_TYPE,
			   ANIMAL_AGE, ANIMAL_WEIGHT, ANIMAL_UNQ, MEMBER_ID
		FROM ANIMAL
		WHERE ANIMAL_NO = #{animal_no}
	</select>
	
	<select id="insert" parameterType="AnimalDto">
		INSERT INTO ANIMAL
		VALUES(ANIMAL_NO_SEQ.NEXTVAL, #{animal_name}, #{animal_gen}, #{animal_type},
			   #{animal_age}, #{animal_weight}, #{animal_unq}, #{member_id})
	</select>
	
	<select id="update" parameterType="AnimalDto">
		UPDATE ANIMAL
		SET ANIMAL_NAME = #{animal_name}
		WHERE ANIMAL_NO = #{animal_no}
	</select>
	
	<select id="delete" parameterType="int">
		DELETE ANIMAL
		WHERE ANIMAL_NO = #{animal_no}
	</select>
	
</mapper>